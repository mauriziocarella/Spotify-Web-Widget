{
	"name": "www",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"start": "dotenv react-scripts start",
		"build": "dotenv -e ../.env react-scripts build && rm -rf ../build && mv ./build ../build",
		"test": "react-scripts test",
		"eject": "react-scripts eject",
		"lint": "../node_modules/.bin/eslint src/**/*.js src/**/*.jsx"
	},
	"devDependencies": {}
}
