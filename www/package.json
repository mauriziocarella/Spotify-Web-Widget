{
	"name": "www",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"start": "react-scripts start",
		"build": "react-scripts build && rm -rf ../build && mv ./build ../build",
		"test": "react-scripts test",
		"eject": "react-scripts eject"
	}
}
